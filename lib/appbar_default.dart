import 'package:flutter/material.dart';

class AppbarDefault extends AppBar {
  AppbarDefault({
    Key? key,
    BuildContext? context,
  }) : super(
          key: key,
          flexibleSpace: Container(), //PaletaCores.gradienteAppBar,
        );
}
